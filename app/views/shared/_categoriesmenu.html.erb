<div class="post_menu">
	
	<h3>Categories</h3>
	
	<ul>
		<% @categories.each do |category| %>
			<li><%= link_to truncate(category.name, :length => 40), category_path(category) %></li>
		<% end %>
	</ul>

</div>